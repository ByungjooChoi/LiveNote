# Gemini Live Translate 참조 프로젝트 7종 분석 리포트

작성일: 2026-08-21 · 목적: livenote2 클라우드 번역 레이어에 참조할 기법 발굴
결론 요약: **치명 버그 1건 발견·수정** (setup 페이로드 스키마), 채택 3건, 백로그 2건, 참고 다수.

---

## 0. 총평

7개 전부 "오디오 스트림 → gemini-3.5-live-translate-preview → 번역 오디오+전사"라는 같은 뼈대를 공유하지만, 성숙도와 배울 점이 크게 갈린다. 가장 값진 것은 **kkdai(macOS·Swift, 우리와 동일 스택)의 실전 함정 기록**과 **Voxis(제품급 Windows 앱)의 운영 기법**이었다. LiveKit 2종은 규모 확장 아키텍처 교과서, 나머지는 아이디어 소스.

| # | 프로젝트 | 스택 | 핵심 가치 | livenote2 반영 |
|---|---|---|---|---|
| 1 | google-gemini/gemini-live-translate-livekit | Next.js + LiveKit | 방송형 다국어 팬아웃, 운영 한계 실측 | 쿼터 지식 (참고) |
| 2 | livekit-examples/gemini-live-translate | Next.js + Python Agents | 화자×언어 쌍별 세션 관리 | 뮤트 유예 개념 (이미 충족) |
| 3 | kkdai/gemini-live-translate-macos | SwiftUI + SCK | **setup 스키마 함정, 재연결 설계** | **채택 (버그 수정)** |
| 4 | msmahdinejad/lingora (Avorythm) | Python + 확장 | fast/precise 이중 경로, 릴리스 위생 | 백로그 |
| 5 | navidseyedain/ALAD | Chrome 확장 | 핸드셰이크 중 오디오 버퍼링 | **채택** |
| 6 | xieyuanqing/vtuber-live-translate | Kotlin/Android | SubtitleStabilizer, 선제 세션 로테이션 | **채택 (로테이션)** |
| 7 | VoxisLive/voxislive | Python/Windows | VAD 게이트, 로테이션, 지연 철학 | **채택 (로테이션)** + 백로그 |

---

## 1. google-gemini/gemini-live-translate-livekit (공식, ~420★, Apache 2.0)

**무엇**: 발표자 1명 → 언어별 Gemini 세션 1개 → 다수 청중이 언어를 골라 듣는 방송형 번역. LiveKit 방에 봇이 들어가 번역 오디오 트랙을 발행한다.

**구조 요점**: 언어당 세션 싱글톤(`TranslationSessionManager`), 전사는 오디오 구독과 분리된 신뢰 데이터 채널로 전송, 탭 닫힘 시 `sendBeacon`으로 세션 정리, 오디오 프레임 직렬 큐(파일업 방지).

**실측 운영 지식 (우리에게 유용)**:
- **무료 티어 API 키는 동시 WebSocket 3~5개 제한.** livenote2는 채널당 1개씩 2세션이라 무료 키로도 동작 범위 안이지만, 여유가 1~3개뿐이다. 향후 세션을 늘리는 설계(예: 화자별 세션)는 유료 티어 전제.
- 언어(세션)당 서버 자원 약 20~30MiB RAM. 세션 자체는 가볍다.
- 세션 타임아웃 1시간 설정 운영.

**판정**: 아키텍처는 방송용이라 우리와 목적이 다름. 쿼터 지식만 취한다.

## 2. livekit-examples/gemini-live-translate (~120★, MIT)

**무엇**: 8인 화상회의에서 각자 언어를 고르면 (화자, 대상언어) 쌍마다 Gemini 세션을 만들어 서로의 말을 각자의 언어로 듣는 P2P형.

**구조 요점**: `attributes.lang` 변경을 감시하는 reconcile 루프(디바운스 250ms)가 필요 세션 집합을 계산해 생성/철거. **뮤트 시 세션 유예 10초** (`SESSION_GRACE_SEC`): 짧은 뮤트 토글로 세션이 출렁이지 않게 함. 원시 v1beta WebSocket + translationConfig 사용 (우리와 동일 프로토콜).

**판정**: 뮤트 유예 개념은 우리 설계가 이미 충족한다 (뮤트 중 세션은 유지하고 전송만 중단). reconcile 패턴은 다중 사용자용이라 해당 없음.

## 3. kkdai/gemini-live-translate-macos (~45★, MIT) — 최고 가치

**무엇**: macOS SwiftUI + ScreenCaptureKit로 특정 앱 오디오를 잡아 같은 모델로 번체중국어 자막+더빙. 우리와 언어 빼고 동일한 문제를 동일한 스택에서 풀었다.

**실전 함정 3건 (README에 기록, 소스로 검증함)**:
1. **setup 페이로드 스키마**: `inputAudioTranscription`/`outputAudioTranscription`은 **setup 루트**에 둬야 한다. 공식 문서의 WebSocket 예제는 generationConfig 안에 넣고 있는데 그대로 하면 CloseCode 1007("Unknown name at 'setup.generation_config'")로 거부된다. translationConfig는 generationConfig 안이 맞다. **→ livenote2의 어제 구현이 정확히 이 함정을 밟고 있었음. 수정 완료 (치명 버그).** 참고로 LiveNote1 S2ST 시절 코드도 전사 설정을 루트에 뒀었다. 공식 문서 예제가 틀린 것.
2. WebSocket 양방향 엔드포인트는 `gemini-3.5-flash`를 지원하지 않음 (1008). live 계열 모델명만 핸드셰이크 성공.
3. SCK 다채널 AudioBufferList는 정적 할당 시 무음 잘림 → 이중 호출 동적 할당 필요 (우리는 프로세스 탭 제로카피라 해당 없음).

**재연결 설계 (채택)**: 지수 백오프 2s·4s·…상한 30s, 최대 10회. **CloseCode 1008(키/모델 문제)은 재연결하지 않고 영구 실패 처리** (설정 문제를 재시도로 가리지 않음). 소켓 아이덴티티 가드, 30초 ping, 연결마다 새 URLSession(CFNetwork 경합 SIGSEGV 회피 실측).

**판정**: setup 수정 + 지수 백오프 채택 완료. 1008 구분과 연결별 URLSession은 백로그 (URLSession.shared 사용 중이라 위험도 낮음).

## 4. msmahdinejad/lingora → Avorythm (~35★, MIT)

**무엇**: 데스크톱(Python)+브라우저 확장. 라이브 번역·더빙에 더해 업로드 미디어 처리(Media Studio)까지. 페르시아어/영어 이중 문서화, CI, 서명 정책(SignPath), SHA256SUMS.

**우리에게 유용한 개념**:
- **fast/precise 이중 경로**: 빠른 경로는 Gemini 3.5 Live 직행, 정밀 경로는 Whisper 전사 → 텍스트 풀 → Gemini 재번역으로 완전 발화 단위를 맞춤. livenote2의 "회의 후 정밀 재처리" 백로그(화자 재처리·재번역)와 같은 철학의 선례.
- 릴리스 위생: SHA256SUMS 단일 파일, 미서명 빌드를 파일명에 명시(`-unsigned`), 코드서명 정책 문서. 우리 dist 체계의 다음 단계 참고.
- 키는 OS 키링 보관 (우리도 Keychain ✓).

## 5. navidseyedain/ALAD (~15★, MIT)

**무엇**: Chrome 확장. 탭 오디오 → Gemini → 더빙 재생. 로컬 사용 통계 대시보드(히트맵)가 특이점.

**채택한 것**: **핸드셰이크 전 오디오 버퍼링** — WebSocket 연결·setup 완료 전에 도착한 오디오를 큐에 쌓았다가 연결되면 방출해 "첫 문장 유실"을 막는다. livenote2는 ready 전 오디오를 그냥 버리고 있었다. → 최근 3초 보관 후 setupComplete에서 방출하도록 수정. 이 버퍼는 선제 로테이션의 공백도 함께 메운다.

**참고**: session resumption을 쓴다고 주장하나 검증 못 함. LiveNote1 시절 S2ST에선 resumption이 오히려 stall을 유발했으므로 (스펙 §7의 교훈) 채택 보류.

## 6. xieyuanqing/vtuber-live-translate (~12★, 라이선스 없음 — 코드 참조 금지, 개념만)

**무엇**: Android 개인 동시통역 앱. 마이크 동시통역 + 앱 내 재생 오디오 캡처, 시스템 오버레이 자막. 문서화가 대단히 성실 (개발일지, 기술기록, CLAUDE.md까지).

**우리에게 유용한 개념**:
- **`GeminiLiveClient`의 "주동 로테이션(主動輪換)"**: 세션 수명 한계에 걸리기 전에 선제적으로 교체. → **채택** (아래 Voxis와 함께).
- **`SubtitleStabilizer`**: 스트리밍 자막의 문장 절단·중복 제거·확정 행 처리를 전담 컴포넌트로 분리하고 회귀 테스트를 붙임. 우리 stabilizedFinalText와 같은 문제의식이고, 테스트 붙이는 관행이 배울 점.
- 다중 API 키(쉼표 구분)와 커스텀 Base URL(호환 프록시) 옵션, 장면(프롬프트 프리셋) 라이브러리 개념. 장면 라이브러리는 요약(Qwen) 쪽에 계정별 컨텍스트 프리셋으로 이식하면 어울린다.

## 7. VoxisLive/voxislive (~8★, PolyForm NC — 코드 재사용 불가, 개념만) — 제품 성숙도 최고

**무엇**: Windows 상용(MS Store)+오픈소스 BYOK 이중 빌드. 시스템 오디오 → Silero VAD 게이트 → Gemini → 리미터 딸린 스테레오 재생. 회의 모드는 가상 마이크로 양방향 통역까지.

**우리에게 유용한 기법 (다수 채택/백로그)**:
- **선제 세션 로테이션** (`session_rotate_minutes`, "15분 상한 전 교체") → **채택**: livenote2에 8분 주기 선제 재연결 추가. goAway 반응형에만 의존하던 것을 보완. LiveNote1 실측(8~10분 리셋)과도 부합.
- **Silero VAD 게이트로 발화만 클라우드 전송** → 백로그: 우리 RMS 게이트(0.004 + 1s hangover)의 상위 호환. FluidAudio에 VAD 모델이 이미 있으므로 이식 비용 낮음. 스펙의 "Phase 2에서 Silero VAD 교체" 계획과 합류.
- 연결 워밍업(캡처 전 핸드셰이크 선행), WebSocket 압축 비활성, drop-oldest 입력 버퍼.
- **지연에 대한 정확한 철학**: 이 모델의 수 초 지연은 통역사의 ear-voice span처럼 설계된 것이라 클라이언트에서 줄일 수 없다. 클라이언트가 할 일은 엔드포인팅 설정을 보내지 않고 연속 스트림을 먹이는 것뿐 (우리 설계와 일치, 안심 근거).
- 자기 출력 재번역 방지: 캡처에서 자기 프로세스 제외 (우리는 번역 오디오를 재생하지 않으므로 해당 없음. 만약 향후 더빙 기능을 넣으면 CATapDescription의 프로세스 제외를 써야 함).
- 경고 사례: 이 리포지토리를 사칭한 악성 설치본 배포 사례 있음. 공개 배포 시 참고.

---

## 8. livenote2에 반영한 것 (이번 커밋)

1. **[치명 수정] setup 페이로드 스키마**: 전사 설정 2개를 setup 루트로 이동 (kkdai 검증). 이전 구현은 첫 클라우드 연결에서 1007로 실패했을 것.
2. **핸드셰이크·로테이션 중 오디오 버퍼링**: 최근 3초 보관 → setupComplete에서 방출 (ALAD).
3. **선제 세션 로테이션**: 채널별 8분 주기 재연결, 버퍼가 공백을 메움 (Voxis·vtuber).
4. **지수 백오프 재연결**: 2s×시도, 상한 30s, 최대 5회 (kkdai).

## 9. 백로그로 넘긴 것 (우선순위 순)

1. Silero VAD 게이트 (FluidAudio VAD 이식) — 클라우드 전송 게이트 + §5.1 문장 분리 개선을 한 번에
2. CloseCode 1008 구분 처리 (키/모델 오류는 재연결 대신 즉시 안내)
3. 릴리스 위생 고도화: SHA256SUMS 통합, 미서명 표기 (Avorythm)
4. 요약용 계정별 컨텍스트 프리셋 (vtuber의 장면 라이브러리 개념)
5. session resumption 실험 (ALAD 주장, S2ST 시절 부작용 전례 있어 신중히)

### 출처

[gemini-live-translate-livekit](https://github.com/google-gemini/gemini-live-translate-livekit) · [livekit-examples/gemini-live-translate](https://github.com/livekit-examples/gemini-live-translate) · [kkdai/gemini-live-translate-macos](https://github.com/kkdai/gemini-live-translate-macos) (README + GeminiLiveConnection.swift 정독) · [Avorythm](https://github.com/msmahdinejad/lingora) · [ALAD](https://github.com/navidseyedain/ALAD) · [vtuber-live-translate](https://github.com/xieyuanqing/vtuber-live-translate) · [voxislive](https://github.com/VoxisLive/voxislive)
